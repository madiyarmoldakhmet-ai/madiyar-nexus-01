name: nexus
description: "Madibook — A global peer-to-peer skill-sharing platform. Human Library powered by Madi-Credits."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.2.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  uuid: ^4.4.0
  google_fonts: ^6.2.1
  intl: ^0.19.0
  http: ^1.6.0
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  agora_rtc_engine: ^6.5.3
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
